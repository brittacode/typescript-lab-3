import { ShippingContainer } from "./shippingcontainer";

export function findContainersByDestination(containers:ShippingContainer [], destination:string) {
    return containers.filter((value: ShippingContainer )  => {
        

    })
}